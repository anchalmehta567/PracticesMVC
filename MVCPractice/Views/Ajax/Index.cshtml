
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<html>
<body>
    <div>
        <h1>Hello</h1>
        
        @* <label id="lbl"></label>
        <input type="text" id="textbox"/>
       *@ 
        @*<input type="button"  onclick="BtnClick()" value="click me" />
*@
    <form>
        <input type="text" id="stdid" placeholder="id" />
        <br />
        <br />

        <input type="text" id="stdname" placeholder="id" />
        <br />
        <br />
        <input type="text" id="stdEmail" placeholder="id" />
        <br />
        <br />
        <input type="button" onclick="BtnClick()" value="Send data"/>
    </form>
    </div>
</body>
</html>
<script>
    @*$(document).ready(function () {
        alert("inside the dom")
      
    });
    *@
    function BtnClick() {
        var id = $("#stdid").val();
        var name = $("#stdname").val();
        var Email = $("#stdEmail").val();

        var obj = {
            id: id,
            name: name,
            Email: Email
        }
        SubmitForm(obj);
    }
    function SubmitForm(obj)
    {
        $.ajax({
            url: "/Ajax/AddStudent",
            method: "POST",
            data:obj,
            success: function (data)
            {
              
                alert(data);
                @*data = JSON.parse(data);
                $("#stdId").text(data.id);
                $("#stdname").text(data.name);
                $("#stdemail").text(data.Email);
*@
            },
            error: function (err)
            {
                console.error(err);
            }
        })
@*
        var value = $("textbox").val();


        $("#lbl").text("I am inside the label by jquery");*@
    }
</script>